[
  {
    "type": "function",
    "function": {
      "name": "view_website_source",
      "description": "Fetches and returns the full, JavaScript-rendered HTML source code from a given URL using a headless Chrome browser. Use this to inspect the structure and content of a webpage.",
      "parameters": {
        "type": "object",
        "properties": {
          "url": {
            "type": "string",
            "description": "The full URL of the website to get the source code from (e.g., 'https://www.example.com')."
          }
        },
        "required": ["url"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "visit_url",
      "description": "Visits a given URL using a headless Chrome browser to simulate a user visit and trigger on-load events. It returns a success or failure message. Use this when an action requires simply loading a page.",
      "parameters": {
        "type": "object",
        "properties": {
          "url": {
            "type": "string",
            "description": "The full URL of the website to visit."
          }
        },
        "required": ["url"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "click_element",
      "description": "Finds and clicks an element (like a button or link) on a webpage. The element is located using its HTML tag, CSS classes, and the visible text it contains.",
      "parameters": {
        "type": "object",
        "properties": {
          "url": {
            "type": "string",
            "description": "The URL of the page where the element to be clicked exists."
          },
          "tag_name": {
            "type": "string",
            "description": "The HTML tag of the element (e.g., 'button', 'a', 'div')."
          },
          "class_names": {
            "type": "string",
            "description": "A space-separated string of the CSS classes on the element (e.g., 'btn btn-primary start-button')."
          },
          "text_content": {
            "type": "string",
            "description": "The visible text within the element that helps to identify it."
          }
        },
        "required": ["url", "tag_name", "class_names", "text_content"]
      }
    }
  }
]